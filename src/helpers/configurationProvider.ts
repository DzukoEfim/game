export class ConfigurationProvider {
    // TODO change to static method or singleton object
    // eslint-disable-next-line class-methods-use-this
    public isGridAvailable() {
        return true;
    }

    // TODO change to static method or singleton object
    // eslint-disable-next-line class-methods-use-this
    public isGridAvaialable() {
        return true;
    }
}
